{
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.5",
    "aws-lambda": "^1.0.7",
    "eslint": "^8.36.0",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "build": "cp package*.json dist/ && npm install --production --prefix dist/ && npx tsc"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.296.0",
    "@aws-sdk/util-dynamodb": "^3.296.0",
    "crc-32": "^1.2.2",
    "fp-ts": "^2.13.1",
    "io-ts": "^2.2.20"
  }
}
